import styled from "styled-components";
import { SnsLoginContainer } from "../../Login/components/SnsLogin";
import SnsLogin from "../../Login/components/SnsLogin";

function SnsSignup() {
  return <SnsLogin />;
}

export default SnsSignup;
